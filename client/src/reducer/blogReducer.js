const blogReducer = (state, action) => {
if (action.type==="ANC") {
  // let { id, category, title, caption, image,  blog} = action.payload;

    return {
      ...state
    }
}
    return state;
  };
  
  export default blogReducer;